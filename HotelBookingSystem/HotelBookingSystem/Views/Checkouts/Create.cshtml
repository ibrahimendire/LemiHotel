@using HotelBookingSystem.ViewModels;
@model AddCheckoutViewModel;

<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @*  <div class="form-group">
            <label asp-for="GuestId" class="control-label"></label>
            <select asp-for="GuestId" class="form-control" asp-items="ViewBag.GuestId"></select>
        </div>*@

            <div class="form-group">

                <label asp-for="Note" class="control-label">Note :</label><br />
                <input asp-for="Note" class="form-control" placeholder="Check in By ..." />
                <span asp-validation-for="Note" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="GuestId" class="control-label">look Guest Last Name:</label><br />
                <select asp-for="GuestId" class="form-control" asp-items="Model.Guests"></select>
            </div>

            <div class="form-group">
                <input type="submit" value="Check In" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-success"> <i class="fas fa-undo-alt"></i> &nbsp; Back to List</a>
            </div>
        </form>
    </div>
</div>


@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

